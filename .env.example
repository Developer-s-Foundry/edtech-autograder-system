# ==========================================
# EdTech Autograder - Environment Template
# ==========================================
# Copy this file to .env and fill in the values.
# Do NOT commit your real .env file to version control.

# ------------------------------------------
# Application Settings
# ------------------------------------------
APP_NAME=EdTech Autograder
ENV=dev                  # dev | staging | production
LOG_LEVEL=INFO           # DEBUG | INFO | WARNING | ERROR


# ------------------------------------------
# Database Configuration
# ------------------------------------------
# Format:
# postgresql+psycopg://<username>:<password>@<host>:<port>/<database_name>
#
# Example (local development):
# postgresql+psycopg://postgres:postgres@localhost:5432/autograder

DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/autograder


# ------------------------------------------
# Redis / Celery Configuration
# ------------------------------------------
# You can use:
# 1. Local Redis (recommended for development)
#    redis://localhost:6379/0
#
# 2. Hosted Redis (e.g., Redis Cloud)...This is the one I'm using for now since
#... I don't want to set up Redis locally.
#    Steps:
#    - Create a free account on Redis Cloud.
#    - Create a database instance.
#    - Copy the provided public endpoint, port, and password.
#    - Replace the placeholders below.

REDIS_URL=redis://default:<password>@<public_url>:<port>/0

# Celery configuration
CELERY_BROKER_URL=${REDIS_URL}
CELERY_RESULT_BACKEND=${REDIS_URL}


# ------------------------------------------
# Judge0 API Configuration
# ------------------------------------------
# If using RapidAPI-hosted Judge0:
# - Create a RapidAPI account.
# - Subscribe to Judge0 CE API.
# - Copy your API key and host.

JUDGE0_BASE_URL=https://judge0-ce.p.rapidapi.com
JUDGE0_API_KEY=your_rapidapi_key_here
JUDGE0_RAPIDAPI_HOST=judge0-ce.p.rapidapi.com

## Generate a secure JWT secret key:
```bash
python -c "import secrets; print(secrets.token_urlsafe(32))"
```

JWT_SECRET_KEY=your_generated_secret_key_here
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7
